{
  "title": "frontmatter/contracts/audience-token",
  "description": "Extension contract for audience tokens. Supports canonical tokens and x-* extension tokens.",
  "$id": "https://raw.githubusercontent.com/calan-co/doc-vader/main/schemas/frontmatter/support/contracts/audience-token.contract/current",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "oneOf": [
    {
      "type": "string",
      "maxLength": 64,
      "pattern": "^x-[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    {
      "type": "string",
      "maxLength": 64,
      "not": {
        "pattern": "^x-[a-z0-9]+(?:-[a-z0-9]+)*$"
      },
      "pattern": "^[A-Za-z][A-Za-z0-9]*(?:-[A-Za-z0-9]+)*$"
    }
  ],
  "version": "1.0.0"
}