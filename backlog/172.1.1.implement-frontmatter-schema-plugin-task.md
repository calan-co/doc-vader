---
id: wi-60284
title: Implement Ajv-backed remark-frontmatter-schema plugin
summary: Create a remark plugin for frontmatter schema validation using Ajv and schema cache.
owner: change-manager
assignee: '@change-manager'
type: work-item
subtype: task
lifecycle: active
status: ready-for-review
priority: high
estimated: 4
links:
  parent:
    - '[[172.1.unify-frontmatter-validation-story]]'
  evidence:
    - '[[record-20260518-124800-60284]]'
  pull_requests:
    - 'https://github.com/calan-co/doc-vader/pull/55'
ordinal: 5
---

## Statement

Implement a remark plugin that validates frontmatter against schemas using Ajv, with caching keyed by path and mtime.

## Prompt

Reference [[remark-lint-unified-architecture.md]] for plugin contract and caching strategy.

## Acceptance Criteria

- [x] Plugin validates required fields and types
- [x] Schema cache implemented

## Implementation Notes

- Use Ajv and file mtime for cache invalidation
- Cache compiled validators by schema path + mtime to avoid recompilation

## Tasks

- [x] Wire `remarkFrontmatterSchema` plugin into `createTiabProcessor`
- [x] Add `frontmatterSchema` option to `TiabProcessorOptions` interface
- [x] Write test suite for `remark-frontmatter-schema` plugin
