---
title: "Phase B: PR-link resolver and condition taxonomy"
id: "210.1.2"
summary: Extract vendor adapter pattern, implement BacklogAutomationProvider interface and GitHubBacklogAutomationProvider, add LinkedPullRequestsResolver strategy.
owner: change-manager
type: work-item
subtype: story
lifecycle: active
status: in-progress
priority: high
assignee: "@change-manager"
estimated: "6h"
tags:
  - backlog-automation
  - architecture
  - vendor-abstraction
  - resolver
links:
  - parent: "[[210.1.vendor-adapter-and-scan-lifecycle-feature.md]]"
  - depends: "[[210.1.1.phase-a-backlog-scan-command-story.md]]"
  - precedes: "[[210.1.3.phase-c-evidence-generation-mode-story.md]]"
  - pull_request: "https://github.com/calan-co/doc-vader/pull/19"
ordinal: 2
---

## statement

As an architect, I want to extract vendor-specific logic into a `BacklogAutomationProvider` interface and implement a multi-strategy `SubjectResolverChain` so that the domain layer is vendor-agnostic and subject resolution is robust with fallback strategies.

## Context

Phase A implemented the scan pipeline with GitHub-specific event parsing hardcoded into the executor. Phase B abstracts this by:
1. Defining `BacklogAutomationProvider` interface for vendor-specific operations
2. Extracting GitHub logic into `GitHubBacklogAutomationProvider`
3. Implementing `SubjectResolver` interface and strategies
4. Adding `LinkedPullRequestsResolver` that fetches PR metadata via provider

## Goals

- Define `BacklogAutomationProvider` interface
- Implement `GitHubBacklogAutomationProvider`
- Define `SubjectResolver` and `SubjectResolverChain` interfaces
- Implement `PayloadSubjectTokensResolver` (current behavior)
- Implement `LinkedPullRequestsResolver` (PR metadata fallback)
- Update `BacklogScanExecutor` to use provider and resolver chain
- Add full condition taxonomy to scan report
- Structured error reporting for resolver failures

## Tasks

- [ ] Define `BacklogAutomationProvider` interface with methods:
  - `parsePayload(raw)`
  - `extractPRIdentity(event)`
  - `extractWorkflowRunIdentity(event)`
  - `normalizePRReference(ref)`
  - `fetchPRMetadata(identity)` (optional)
- [ ] Create provider registry and initialization logic
- [ ] Extract GitHub event parsing from Phase A into `GitHubBacklogAutomationProvider`
- [ ] Define `SubjectResolver` interface
- [ ] Implement `PayloadSubjectTokensResolver`
- [ ] Implement `LinkedPullRequestsResolver` (uses provider to fetch PR)
- [ ] Implement `SubjectResolverChain` executor with strategy ordering
- [ ] Update `BacklogScanExecutor` to use resolver chain
- [ ] Add condition evaluations: `subject_resolved`, `pr_link_found`, `valid_evidence`
- [ ] Add error types: `resolve_subject_failed`, `fetch_pr_metadata_failed`
- [ ] Document condition and error taxonomy
- [ ] Unit tests for each resolver strategy (mock provider)
- [ ] Integration tests for resolver chain
- [ ] Update scan report to include resolution details (strategy used, attempts)

## Acceptance Criteria

- [ ] `BacklogAutomationProvider` interface defined with all required methods
- [ ] `GitHubBacklogAutomationProvider` implements interface correctly
- [ ] `SubjectResolver` interface and all strategies implemented
- [ ] `SubjectResolverChain` tries strategies in order; uses first successful result
- [ ] Resolver chain includes detailed attempt log in scan report
- [ ] `LinkedPullRequestsResolver` fetches PR metadata and extracts subjects
- [ ] Error handling: resolver failures reported with reason (not silent)
- [ ] All conditions from taxonomy present in scan report
- [ ] No GitHub-specific logic outside provider implementation
- [ ] Example: templ.js incident (missing subjects) now resolved via PR-link strategy
- [ ] Unit tests cover all resolver strategies and chain logic
- [ ] Provider tests mock GitHub API calls

## Deliverables

- `lib/backlog-automation/provider.ts`: `BacklogAutomationProvider` interface and registry
- `lib/backlog-automation/providers/github.ts`: `GitHubBacklogAutomationProvider` implementation
- `lib/backlog-automation/resolver.ts`: `SubjectResolver` interface and `SubjectResolverChain`
- `lib/backlog-automation/resolvers/payload-tokens.ts`: `PayloadSubjectTokensResolver`
- `lib/backlog-automation/resolvers/linked-pr.ts`: `LinkedPullRequestsResolver`
- `lib/backlog-automation/conditions.ts`: Updated with full condition taxonomy
- `tests/backlog-automation/provider.spec.ts`: Provider tests
- `tests/backlog-automation/resolver.spec.ts`: Resolver tests
- `tests/backlog-automation/integration-b.spec.ts`: Phase B integration test

## Dependencies

- Depends on Phase A: `BacklogScanExecutor` and `ScanReporter` from Phase A
- GitHub API client (already available in project)

## Configuration

- Add optional `automation.subjectResolutionOrder` to `.doc-vader/backlog-consumer.json` schema
- Defaults to: `["payload_subject_tokens", "linked_pull_requests"]`

## Validation

- [ ] All resolver strategies tested independently
- [ ] PR metadata fetching handles API errors gracefully
- [ ] Chain stops on first successful resolution
- [ ] Resolver failure does not prevent scan completion (errors reported)
- [ ] Example: templ.js PR #123 body contains `work-item:175` (not in payload); LinkedPullRequestsResolver finds it

## Related ADRs

- ADR-002: Vendor Adapter Pattern
- ADR-003: Multi-Strategy Subject Resolution

## Notes

Phase B introduces a critical design separation: vendor-specific logic (GitHub API, payload parsing) is now in provider; domain logic (condition checking, resolver orchestration) is vendor-agnostic. This unblocks future vendor additions without domain layer changes.

