# OpenAI API key
OPENAI_KEY=

# Custom markdown issue tracker
# No tracker-specific API token is required. Sandcastle reads and updates
# backlog/*.md files from the mounted repository using the logged-in CLI session.
