---
"@calan-co/doc-vader": patch
---

Run Sandcastle sandbox install and build setup as one ordered hook so build cannot race dependency installation, and make Sandcastle prompt adapter commands non-interactive.
